

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="stylesheets/style.css">
    <title>All the Recipes</title>
    <%- include('../partials/head') %>
</head>
<body>

    <h1>Recipes</h1>

    <ul>
        <% foundrecipe.forEach((recipe) => { %>
            <li>
                <h2>
                    <a href="/recipes/<%= recipe._id %>" class="test">
                        <%= recipe.title %>
                    </a>
                </h2>
                <p><%= recipe.description %></p>
                <p><strong><%= recipe.price %> BHD</strong></p>
                <img src="<%= recipe.image %>" class="recipes-img" alt="Image of <%= recipe.title %>" />
            </li>
        <% }) %>
    </ul>
</body>
</html>